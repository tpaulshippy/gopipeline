{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and run",
            "type": "shell",
            "command": "go build && ./gopipeline",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}